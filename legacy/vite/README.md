# Vite archive

Previous Vite implementation and historical pnpm lockfile. Preserved for reference
and excluded from npm workspaces, builds and lint. The active applications are in
`apps/frontend` and `apps/backend`.
